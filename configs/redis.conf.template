masterauth "{{ secret "REDIS_SWARM_SECRET" }}"
requirepass "{{ secret "REDIS_SWARM_SECRET" }}"

dir "/data"

# RDB
dbfilename "dump.rdb"

# AOF
appendonly yes
appendfilename "appendonly.aof"

# Redis replication configs
replica-announce-ip ${REDIS_HOSTNAME_PREFIX}${REDIS_SWARM_SLOT}
${REDIS_SLAVEOF}
